# API

!!! todo "Stub — to be written"
    The control API. Merge `_context/api-spec.md` (WebSocket JSON protocol,
    port 81) with `../animation/_context/runtime-api-and-control.md` (gait
    playback control).

## WebSocket protocol

!!! todo
    Connection, port 81, message envelope, JSON schema.

## Message types

!!! todo
    Per-message-type table: command, payload, response.

## Runtime / gait control

!!! todo
    Playback control surface from the gait-design runtime spec.
