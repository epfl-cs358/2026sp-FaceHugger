# `.fhc` clip format

!!! todo "Stub — to be written"
    The on-board animation clip format and player. The biggest dedup job —
    merge `_context/onboard-clip-player-design.md` (primary),
    `_context/gait-file-format.md`, `_context/esp32-playback-engine.md`,
    `_context/leg-coordinates.md`, and `_context/api-surface.md`.

## What `.fhc` is

!!! todo
    One-shot animation clip bundle for on-board playback; replaces the `.gait`
    baked-angle format and textbook IK in firmware.

## File format

!!! todo
    Bundle layout, header, per-frame data.

## ESP32 playback engine

!!! todo
    How the firmware plays a clip; memory constraints.

## Leg coordinate conventions

!!! todo
    The coordinate canon from `leg-coordinates.md`.
